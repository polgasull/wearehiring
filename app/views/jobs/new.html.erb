<section class="section main-section">
  <%= link_to jobs_path, class: "button" do %>
    <span class="icon is-small">
      <%= icon('fas', 'arrow-left') %>
    </span>
    <span> <%= t('back') %> </span>
  <% end %>
  <div class="container">
    <div class="columns">
      <div class="column is-8 is-offset-2">
        <h1 class="title is-3"> <%= t('jobs.post_job') %> ¡Al instante! </h1>
        <%= render 'form', job: @job %>
      </div>
    </div>
  </div>
</section>

<%= content_for :footer do %>

  <%= javascript_include_tag "jobs/stripe.js" %>
  <%= javascript_include_tag "jobs/jobs.js" %>

<% end %>