<section class="section main-section">
  <div class="container">
    <%= link_to jobs_path, class: "button is-info" do %>
      <span class="icon is-small">
        <%= icon('fas', 'arrow-left') %>
      </span>
      <span> <%= t('back') %> </span>
    <% end %>
  </div>
  <div class="mv4 is-mobile-only"></div>
  <div class="container">
    <div class="columns">
      <div class="column is-8 is-offset-2">
        <h1 class="title is-3"> <%= t('jobs.post_job') %> ¡Al instante! </h1>
        <%= render 'form', job: @job %>
      </div>
    </div>
  </div>
</section>

<%= content_for :footer do %>

  <%= javascript_include_tag "jobs/stripe.js" %>

<% end %>