<% if (params.has_key?(:company)) %>
    <%= render partial: "/devise/registrations/company_signup", 
    locals: { resource: resource, resource_name: resource_name } %>
<% end %>

<% if (params.has_key?(:candidate)) %>
    <%= render partial: "/devise/registrations/candidate_signup", 
    locals: { resource: resource, resource_name: resource_name } %>
<% end %>