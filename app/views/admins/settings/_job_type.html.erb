<%= simple_form_for(job_type, method: :patch, url: admins_job_type_path(job_type.id), html: { multipart: true }) do |f| %>
  <div class="columns is-vertical-center listrow">
    <div class="column has-text-centered">
      <%= job_type.id %>
    </div>
    <div class="column">
      <%= f.input :name, label: false, input_html: { class: "input" }, wrapper: false, placeholder: t('categories.name') %>
    </div>
    <div class="column">
      <%= job_type.internal_name %>
    </div>
    <div class="column">
      <%= f.submit t('save'), class: 'button is-info' %>
    </div>
  </div>
<% end %>