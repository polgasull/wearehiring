<% headers = ['Name', 'Cargo actual', 'Teléfono', 'Email', 'Linkedin URL'] %>
<%= raw CSV.generate_line headers %>
<% @inscriptions.each do |inscription| %>
  <%= raw CSV.generate_line([inscription.user.name, 
      inscription.user.current_position, 
      inscription.user.phone, 
      inscription.user.email, 
      inscription.user.profile_url]) %>
<% end %>