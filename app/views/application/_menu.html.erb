<nav class="navbar" role="navigation" aria-label="main navigation">
	<div class="navbar-end">
		<div class="navbar-brand">
			<%= link_to t('show_jobs'), 
					jobs_path(locale:params[:locale] || 'es'), 
					class: "navbar-item #{ 'active' if current_page?(jobs_path)}" %>
		</div>
		<div class="navbar-brand">
			<%= link_to t('jobs.post_job'), 
					new_job_path, 
					class: "navbar-item #{ 'active' if current_page?(new_job_path)}" %>
		</div>
	<% if user_signed_in? %>
		<div class="navbar-brand">
			<%= link_to t('company_profile'), 
									edit_user_registration_path, 
									class: "navbar-item #{ 'active' if current_page?(edit_user_registration_path) || current_page?(user_jobs_jobs_path)}" %>
		</div>
		<div class="navbar-brand">
			<%= link_to t('logout'), 
									destroy_user_session_path, 
									method: :delete, 
									class: "navbar-item" %>
		</div>
	<% end %>
	</div>
</nav>