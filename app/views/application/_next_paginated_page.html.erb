$("#pagination-list").append("<%= j render(partial: partial, collection: collection, as: alias_) %>")
$("#pagination").html("<%= j link_to_next_page collection, 'Next page'%>")
$("#pagination-loader").hide()


<% if collection.last_page? %>
  $('#pagination').remove();
  $(".no-more").show()
<% end %>